# QuickWord keeps nothing extra: no reflective serialization, no JNI, no
# Class.forName. Android components are kept by the manifest, Compose by its
# consumer rules. Add a keep rule here only with a release-build repro that
# proves it is needed.
